interface:
  display_name: "Medical ML Leakage Guard"
  short_description: "Strict medical prediction leakage and lineage guard"
  default_prompt: "Run a publication-grade medical prediction review with strict request contract validation, phenotype-definition and transitive lineage leakage checks, pinned metric-path consistency validation, permutation falsification, reproducibility comparison, and fail-closed evidence outputs."
