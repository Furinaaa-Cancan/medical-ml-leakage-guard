interface:
  display_name: "Medical ML Leakage Guard"
  short_description: "Strict medical prediction leakage and lineage guard"
  default_prompt: "Run a publication-grade medical prediction review with strict request contract validation, signed execution-attestation verification (key policy, revocation list, trusted timestamp record, transparency log, execution-receipt proof, execution-log attestation, and independent authority keys), TRIPOD+AI/PROBAST+AI/STARD-AI checklist validation, split-protocol verification, covariate-shift/separability audit, class-imbalance policy checks, missing-data/imputation policy checks (including large-scale MICE suitability), hyperparameter-tuning leakage isolation, phenotype-definition and transitive lineage leakage checks, pinned metric-path consistency validation, evaluation-quality checks for primary-metric confidence intervals and baseline comparisons, permutation falsification, reproducibility comparison, and fail-closed evidence outputs."
